/**
 * Test mode: bypass paywall in development builds.
 * Set to true to test premium features (techniques, hints, etc.) without purchasing.
 * Defaults to __DEV__ so dev builds automatically bypass the paywall.
 */
export const TEST_MODE_BYPASS_PAYWALL = __DEV__;
